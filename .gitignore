# 虚拟环境 / 缓存
.venv/
__pycache__/
*.pyc
.DS_Store

# 大体积外部资产 (可通过 upstream/fetch_pinned.sh 按固定 commit 还原)
upstream/gh/

# 临时文件
*.tmp

# pip editable 构建产物
*.egg-info/

# 构建产物 / 测试缓存
dist/
build/
.pytest_cache/
